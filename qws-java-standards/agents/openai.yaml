interface:
  display_name: "QWS Java 开发规范"
  short_description: "按 QWS 规范编写 Java 代码，复用已有实现并控制迭代范围"
  default_prompt: "使用 $qws-java-standards 按 QWS Java 开发规范完成当前任务，优先复用已有实现，并按四类规范检查本次改动。"
policy:
  allow_implicit_invocation: true
